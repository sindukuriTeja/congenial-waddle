specVersion: "v1"
serviceId: "newfile"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newfile-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
